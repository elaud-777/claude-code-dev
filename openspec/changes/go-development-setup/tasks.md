## 1. 모듈 및 프로젝트 구조 초기화

- [ ] 1.1 `go mod init`으로 `go.mod` 생성
- [ ] 1.2 `cmd/`, `internal/` 디렉터리 구조 생성
- [ ] 1.3 최소한의 진입점(`cmd/<app>/main.go`) 및 예시 패키지 작성

## 2. 포맷팅 및 린트 설정

- [ ] 2.1 `golangci-lint` 설정 파일(`.golangci.yml`) 추가
- [ ] 2.2 `gofmt` 검사 명령 정의

## 3. 테스트 구성

- [ ] 3.1 최소 예시 단위 테스트 작성
- [ ] 3.2 `go test ./...` 로 테스트가 통과하는지 확인

## 4. 빌드/린트/테스트 자동화

- [ ] 4.1 `Makefile` 작성 (`build`, `fmt-check`, `lint`, `test` 타겟 포함)
- [ ] 4.2 로컬에서 각 타겟이 정상 동작하는지 확인

## 5. CI 파이프라인 구성

- [ ] 5.1 GitHub Actions 워크플로우 파일 추가 (빌드, 린트, 테스트 단계)
- [ ] 5.2 워크플로우가 push/PR 시 트리거되는지 확인
